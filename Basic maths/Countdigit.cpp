#include<bits/stdc++.h>
using namespace std;
int main(){
    int n;
    cout<<"Enter the number: ";
    cin>>n;
    cout<<"Number of digits are: "<<int(log10(n)+1);
    // int count=0;
    // while(n>0){
    //     n= n/10;
    //     count++;
    // }
   // cout<<"Number of digits are: "<<count;
    return 0;
}